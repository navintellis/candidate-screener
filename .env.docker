# Docker-specific environment variables
# This file contains variables that are commonly overridden in Docker environments

# Node environment
NODE_ENV=localhost

# Server configuration (Docker container settings)
PORT=3000
HOST=0.0.0.0

# Docker-specific port mapping
DOCKER_PORT=3000

# Storage configuration
STORAGE_TYPE=filesystem
CANDIDATE_DATA_PATH=candidate-data
PROCESSED_DATA_PATH=processed-data

# For S3 storage (uncomment and set when using S3)
# S3_BUCKET_NAME=your-bucket-name
# S3_REGION=us-east-1
# S3_ACCESS_KEY_ID=your-access-key
# S3_SECRET_ACCESS_KEY=your-secret-key
# S3_CANDIDATE_DATA_PREFIX=candidate-data
# S3_PROCESSED_DATA_PREFIX=processed-data

# OpenAI Configuration (set these in your actual environment)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_TRANSCRIPTION_MODEL=gpt-4o-transcribe
OPENAI_EXTRACTION_MODEL=gpt-5

# Application Configuration
TIMEZONE=Asia/Kolkata

# Logging Configuration
LOG_LEVEL=info
LOG_ENABLE_CONSOLE=true
